@model IEnumerable<GetDeviceCategoryListResponse>
@{
    ViewData["Title"] = "Home Page";
}

<h1>All Device Categories</h1>
<br />
<br />
<a asp-action="Create" class="btn btn-primary">Add New Device Category</a>
<br />
<br />
@if (Model.Count() > 0)
{
    <div>
        <table class="table table-striped table-hover">
            <thead>
                <tr>
                    <th>@Html.DisplayNameFor(m => m.Name)</th>

                    <th>Details</th>
                    <th>Update</th>
                    <th>Delete</th>
                </tr>
            </thead>
            <tbody>
                @{
                    int index = 0;
                }
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Name</td>

                        <td>
                            <a class="btn btn-info" asp-action="Details" asp-controller="DeviceCategory" asp-route-id="@item.Id">
                                <i class="fas fa-eye"></i>Details
                            </a>
                        </td>
                        <td>
                            <a class="btn btn-success" asp-action="Edit" asp-controller="DeviceCategory" asp-route-id="@item.Id">
                                <i class="fas fa-edit"></i>Update Properties
                            </a>
                        </td>
                        <td>
                            <a class="btn btn-danger" asp-action="Delete" asp-controller="DeviceCategory" asp-route-id="@item.Id">
                                <i class="fas fa-trash"></i>Delete
                            </a>
                        </td>
                    </tr>

                }
            </tbody>
        </table>
    </div>
}
else
{
    <div class="alert alert-warning">
        <h3>No Categories Found!</h3>
    </div>
}